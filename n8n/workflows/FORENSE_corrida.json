{
  "name": "FORENSE_corrida",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "forense/corrida",
        "authentication": "headerAuth",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        0,
        0
      ],
      "name": "Webhook corrida",
      "credentials": {
        "httpHeaderAuth": {
          "name": "Forense Webhook"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Reutiliza la corrida `lista` con la misma idempotency_key o crea una `preparando`. `dataset` selecciona un ORIGEN AUTORIZADO, nunca una URL arbitraria. DEPENDE de forense-db.\nSELECT a.corrida_id, a.estado, a.idempotency_key, a.corrida_origen_id,\n       a.investigacion_id, a.dataset, a.reutilizada\n  FROM forense.abrir_corrida(nullif(nullif($1::text, 'null'), '')::text, nullif(nullif($2::text, 'null'), '')::text, nullif(nullif($3::text, 'null'), '')::uuid) a",
        "options": {
          "queryReplacement": "={{ ($json.body.dataset) ?? 'null' }}, ={{ ($json.body.idempotency_key) ?? 'null' }}, ={{ ($json.body.corrida_origen_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        220,
        0
      ],
      "name": "Validar e idempotencia",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ corrida_id: $json.corrida_id, estado: $json.estado }) }}",
        "options": {
          "responseCode": 202
        }
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        440,
        -140
      ],
      "name": "Responder corrida 202"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Snapshot COMPLETO; con corrida_origen_id clona (regla 10: corridas aisladas). DEPENDE de forense-db.\nSELECT s.corrida_id, s.estado, s.filas_por_tabla, s.corrida_origen_id\n  FROM forense.cargar_o_clonar_snapshot(nullif(nullif($1::text, 'null'), '')::uuid, nullif(nullif($2::text, 'null'), '')::uuid) s",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}, ={{ ($('Validar e idempotencia').first().json.corrida_origen_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        440,
        0
      ],
      "name": "Cargar o clonar snapshot",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Conteos, dataset_hash, fecha_corte y familias_evaluables → `lista`; fallo → `error` con causa. Una corrida vacía no se investiga. DEPENDE de forense-db.\nSELECT v.corrida_id, v.estado, v.dataset_hash, v.fecha_corte,\n       v.familias_evaluables, v.causa\n  FROM forense.verificar_integridad_corrida(nullif(nullif($1::text, 'null'), '')::uuid) v",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        660,
        0
      ],
      "name": "Verificar integridad",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- La función hace el claim atómico de `lista` a `procesando`: n8n NO anticipa el cambio de estado (003).\nSELECT forense.correr_pistas(nullif(nullif($1::text, 'null'), '')::uuid) AS pistas_insertadas",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        880,
        0
      ],
      "name": "Correr pistas",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- DEPENDE de 004 (forense-db).\nSELECT forense.armar_clusters(nullif(nullif($1::text, 'null'), '')::uuid) AS clusters_armados",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        1100,
        0
      ],
      "name": "Armar clusters",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Cola COMPLETA ordenada por score. El predicado es EL MISMO de forense.cola_corrida (012: pendiente y sin caso): si difiriera, `cola_restante` contaría clusters que el despacho nunca elige y el bucle no terminaría. El límite de activos lo impone el nodo siguiente, no este SELECT.\nSELECT k.id AS cluster_id, k.corrida_id, k.score, k.estado,\n       nullif(nullif($2::text, 'null'), '')::uuid AS investigacion_id, nullif(nullif($3::text, 'null'), '')::text AS idempotency_key\n  FROM forense.clusters k\n WHERE k.corrida_id = nullif(nullif($1::text, 'null'), '')::uuid\n   AND k.estado = 'pendiente'\n   AND NOT EXISTS (SELECT 1 FROM forense.casos c WHERE c.cluster_id = k.id)\n ORDER BY k.score DESC NULLS LAST, k.id",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}, ={{ ($('Validar e idempotencia').first().json.investigacion_id) ?? 'null' }}, ={{ ($('Validar e idempotencia').first().json.idempotency_key) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        1320,
        0
      ],
      "name": "Clusters por score",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// Constantes inyectadas por n8n/runtime/generar-workflows.mjs.\n// 17 §2: el límite propio de DB controla ocho pasos y CUATRO clusters. Un límite de n8n no sustituye ese control; lo que no cabe queda EN COLA, no fallido (regla 10).\nconst MAX_ACTIVOS = 4;\n\n// ARCHIVO GENERADO — no editar a mano.\n// Fuente: ../runtime/nodos/despachar-clusters.mjs (región CODE_NODE). Regenerar: node n8n/runtime/generar-code-nodes.mjs\n// Corrida: despacho inicial — admite hasta MAX_ACTIVOS clusters y deja el resto EN COLA (17 §2, regla 10).\n\nconst corrida = $('Validar e idempotencia').first().json;\nconst filas = $input.all().map((i) => i.json);\nconst x = {\n  corrida_id: corrida.corrida_id,\n  investigacion_id: corrida.investigacion_id ?? null,\n  idempotency_base: corrida.idempotency_key,\n  max_activos: MAX_ACTIVOS,\n  casos_activos: 0,\n  pendientes: filas.filter((f) => f && f.cluster_id)\n    .map((f) => ({ cluster_id: f.cluster_id, score: f.score })),\n};\nconst entero = (v, porDefecto) => {\n  const n = Number(v);\n  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : porDefecto;\n};\nconst max = entero(x.max_activos, 0);\nconst activos = entero(x.casos_activos, 0);\nconst libres = Math.max(0, max - activos);\n\n// `pendientes` viene del SELECT que replica el predicado de\n// `forense.cola_corrida`: cluster `pendiente` y SIN caso. El orden (score\n// desc, id) lo fija SQL; aquí no se reordena para que la cola despachada y\n// la cola contada sean la misma lista.\nconst pendientes = (Array.isArray(x.pendientes) ? x.pendientes : [])\n  .filter((p) => p && p.cluster_id);\nconst admitidos = pendientes.slice(0, libres);\nconst en_cola = pendientes.slice(admitidos.length);\n\nconst base = x.idempotency_base ?? '';\nconst comun = {\n  corrida_id: x.corrida_id ?? null,\n  investigacion_id: x.investigacion_id ?? null,\n  max_activos: max,\n  casos_activos: activos,\n  slots_libres: libres,\n  pendientes_totales: pendientes.length,\n  admitidos: admitidos.length,\n  en_cola: en_cola.length,\n  cola_restante: en_cola.map((c) => c.cluster_id),\n};\n\nconst salida = admitidos.length > 0\n  ? admitidos.map((c) => Object.assign({}, comun, {\n    despachar: true,\n    cluster_id: c.cluster_id,\n    // Una clave por (corrida, cluster): reintentar la vuelta no duplica casos.\n    idempotency_key: `${base}:${c.cluster_id}`,\n    motivo: 'slot libre',\n  }))\n  : [Object.assign({}, comun, {\n    despachar: false,\n    cluster_id: null,\n    idempotency_key: null,\n    motivo: pendientes.length === 0 ? 'cola vacia' : 'sin slot libre',\n  })];\nreturn salida.map((json) => ({ json }));"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        0
      ],
      "name": "Despachar hasta 4"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.despachar }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1760,
        0
      ],
      "name": "¿Hay admitidos?"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "mode": "list",
          "value": "PENDIENTE_FORENSE_INVESTIGAR_CLUSTER",
          "cachedResultName": "FORENSE_investigar_cluster"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {
            "cluster_id": "={{ $json.cluster_id }}",
            "corrida_id": "={{ $json.corrida_id }}",
            "investigacion_id": "={{ $json.investigacion_id }}",
            "idempotency_key": "={{ $json.idempotency_key }}"
          }
        },
        "mode": "each",
        "options": {
          "waitForSubWorkflow": false
        }
      },
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        1980,
        0
      ],
      "name": "Despachar cluster"
    },
    {
      "parameters": {
        "jsCode": "const filas = $input.all().map((i) => i.json);\nconst despachados = filas.filter((f) => f && f.despachar === true);\nreturn [{ json: {\n  vuelta: true,\n  items_entrantes: filas.length,\n  despachados_en_vuelta: despachados.length,\n  clusters_en_vuelta: despachados.map((f) => f.cluster_id),\n} }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2200,
        0
      ],
      "name": "Ciclo de corrida"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Reconcilia errores, timeouts y leases, y cuenta TAMBIÉN la cola de clusters (012). Terminar de despachar NO cierra la corrida. `cerrable` añade el único caso en que `terminada` es false y aun así no hay nada que esperar: `sin_clusters` (estado_corrida exige total>0 para declarar terminada, así que una corrida sin clusters giraría para siempre). DEPENDE de forense-db.\nSELECT e.corrida_id, e.terminada, e.estado_final,\n       e.completados, e.en_cola, e.errores,\n       (e.terminada OR e.estado_final = 'sin_clusters') AS cerrable\n  FROM forense.estado_corrida(nullif(nullif($1::text, 'null'), '')::uuid) e",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        2420,
        0
      ],
      "name": "Esperar y reconciliar",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.cerrable }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        2640,
        0
      ],
      "name": "¿Corrida terminada?"
    },
    {
      "parameters": {
        "resume": "timeInterval",
        "amount": "={{ (10000) / 1000 }}",
        "unit": "seconds"
      },
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        2420,
        140
      ],
      "name": "Espera corrida"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Una fila SIEMPRE (cola_corrida devuelve una). `pendientes` usa EL MISMO predicado que cuenta `cola_restante`: los dos números tienen que hablar de los mismos clusters o el bucle gira sin despachar. DEPENDE de db/012.\nSELECT q.corrida_id, q.clusters_total, q.cola_restante, q.casos_activos,\n       COALESCE((SELECT jsonb_agg(jsonb_build_object(\n                          'cluster_id', k.id, 'score', k.score)\n                        ORDER BY k.score DESC NULLS LAST, k.id)\n                   FROM forense.clusters k\n                  WHERE k.corrida_id = nullif(nullif($1::text, 'null'), '')::uuid\n                    AND k.estado = 'pendiente'\n                    AND NOT EXISTS (SELECT 1 FROM forense.casos c\n                                     WHERE c.cluster_id = k.id)),\n                '[]'::jsonb) AS pendientes\n  FROM forense.cola_corrida(nullif(nullif($1::text, 'null'), '')::uuid) q",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        2640,
        140
      ],
      "name": "Cola y slots",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Constantes inyectadas por n8n/runtime/generar-workflows.mjs.\n// El mismo techo del despacho inicial: los slots que libera un caso dictaminado se vuelven a llenar, nunca se superan.\nconst MAX_ACTIVOS = 4;\n\n// ARCHIVO GENERADO — no editar a mano.\n// Fuente: ../runtime/nodos/despachar-clusters.mjs (región CODE_NODE). Regenerar: node n8n/runtime/generar-code-nodes.mjs\n// Corrida: redespacho por vuelta — llena los slots que se liberaron mientras quede cola (hallazgo alto H11).\n\nconst corrida = $('Validar e idempotencia').first().json;\nconst cola = $input.first().json;\nconst x = {\n  corrida_id: corrida.corrida_id,\n  investigacion_id: corrida.investigacion_id ?? null,\n  idempotency_base: corrida.idempotency_key,\n  max_activos: MAX_ACTIVOS,\n  casos_activos: cola.casos_activos,\n  pendientes: cola.pendientes ?? [],\n};\nconst entero = (v, porDefecto) => {\n  const n = Number(v);\n  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : porDefecto;\n};\nconst max = entero(x.max_activos, 0);\nconst activos = entero(x.casos_activos, 0);\nconst libres = Math.max(0, max - activos);\n\n// `pendientes` viene del SELECT que replica el predicado de\n// `forense.cola_corrida`: cluster `pendiente` y SIN caso. El orden (score\n// desc, id) lo fija SQL; aquí no se reordena para que la cola despachada y\n// la cola contada sean la misma lista.\nconst pendientes = (Array.isArray(x.pendientes) ? x.pendientes : [])\n  .filter((p) => p && p.cluster_id);\nconst admitidos = pendientes.slice(0, libres);\nconst en_cola = pendientes.slice(admitidos.length);\n\nconst base = x.idempotency_base ?? '';\nconst comun = {\n  corrida_id: x.corrida_id ?? null,\n  investigacion_id: x.investigacion_id ?? null,\n  max_activos: max,\n  casos_activos: activos,\n  slots_libres: libres,\n  pendientes_totales: pendientes.length,\n  admitidos: admitidos.length,\n  en_cola: en_cola.length,\n  cola_restante: en_cola.map((c) => c.cluster_id),\n};\n\nconst salida = admitidos.length > 0\n  ? admitidos.map((c) => Object.assign({}, comun, {\n    despachar: true,\n    cluster_id: c.cluster_id,\n    // Una clave por (corrida, cluster): reintentar la vuelta no duplica casos.\n    idempotency_key: `${base}:${c.cluster_id}`,\n    motivo: 'slot libre',\n  }))\n  : [Object.assign({}, comun, {\n    despachar: false,\n    cluster_id: null,\n    idempotency_key: null,\n    motivo: pendientes.length === 0 ? 'cola vacia' : 'sin slot libre',\n  })];\nreturn salida.map((json) => ({ json }));"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2860,
        140
      ],
      "name": "Redespachar pendientes"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.despachar }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        3080,
        140
      ],
      "name": "¿Hay pendientes?"
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "mode": "list",
          "value": "PENDIENTE_FORENSE_INVESTIGAR_CLUSTER",
          "cachedResultName": "FORENSE_investigar_cluster"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {
            "cluster_id": "={{ $json.cluster_id }}",
            "corrida_id": "={{ $json.corrida_id }}",
            "investigacion_id": "={{ $json.investigacion_id }}",
            "idempotency_key": "={{ $json.idempotency_key }}"
          }
        },
        "mode": "each",
        "options": {
          "waitForSubWorkflow": false
        }
      },
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        3300,
        140
      ],
      "name": "Despachar pendiente"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- Pese al prefijo v_, el contrato de 05/10 es una FUNCIÓN que devuelve jsonb. `estado_final` se relee aquí: «Esperar y reconciliar» corre muchas veces y una referencia cruzada a una de sus vueltas no es determinista. `estado_cierre` lo traduce al dominio de `corridas.estado` (CHECK: preparando|lista|procesando|completada|error): `sin_clusters` NO es un estado persistible y escribirlo rompía la restricción; el matiz viaja en `metricas.estado_final_calculado`.\nSELECT forense.v_metricas_corrida(nullif(nullif($1::text, 'null'), '')::uuid)\n       || jsonb_build_object('estado_final_calculado', e.estado_final) AS metricas,\n       e.estado_final, e.terminada,\n       CASE WHEN e.estado_final = 'error' THEN 'error' ELSE 'completada' END AS estado_cierre\n  FROM forense.estado_corrida(nullif(nullif($1::text, 'null'), '')::uuid) e",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        2420,
        0
      ],
      "name": "Métricas",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- `completada` con conteos completados/en cola/error y cobertura; `error` si falla antes de tener resultados utilizables. Una corrida sin clusters cierra `completada` con `metricas.estado_final_calculado = sin_clusters`: no se investigó nada, pero tampoco falló nada.\nUPDATE forense.corridas\n   SET estado = nullif(nullif($2::text, 'null'), '')::text, fin = now(), metricas = $3::jsonb\n WHERE id = nullif(nullif($1::text, 'null'), '')::uuid\nRETURNING id AS corrida_id, estado, fin, metricas",
        "options": {
          "queryReplacement": "={{ ($('Validar e idempotencia').first().json.corrida_id) ?? 'null' }}, ={{ ($json.estado_cierre) ?? 'null' }}, ={{ (JSON.stringify($json.metricas)) ?? 'null' }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        2640,
        0
      ],
      "name": "Cerrar corrida",
      "credentials": {
        "postgres": {
          "name": "Forense Postgres"
        }
      }
    },
    {
      "parameters": {
        "content": "FORENSE_corrida (07 §1, 17 §2, regla 10).\n\nMAX_ACTIVOS clusters activos como máximo; el resto queda EN COLA\ny se despacha en la rama de espera conforme se liberan slots.\nUna corrida vacía no se investiga y terminar de despachar\nno es cerrar la corrida: cierra `estado_corrida`, que cuenta\ntambién la cola de clusters (012). Sin clusters, `terminada`\nes false por definición: lo cierra `cerrable`.",
        "height": 240,
        "width": 400,
        "color": 4
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        0,
        420
      ],
      "name": "Nota corrida"
    }
  ],
  "connections": {
    "Webhook corrida": {
      "main": [
        [
          {
            "node": "Validar e idempotencia",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validar e idempotencia": {
      "main": [
        [
          {
            "node": "Responder corrida 202",
            "type": "main",
            "index": 0
          },
          {
            "node": "Cargar o clonar snapshot",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cargar o clonar snapshot": {
      "main": [
        [
          {
            "node": "Verificar integridad",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verificar integridad": {
      "main": [
        [
          {
            "node": "Correr pistas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Correr pistas": {
      "main": [
        [
          {
            "node": "Armar clusters",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Armar clusters": {
      "main": [
        [
          {
            "node": "Clusters por score",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Clusters por score": {
      "main": [
        [
          {
            "node": "Despachar hasta 4",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Despachar hasta 4": {
      "main": [
        [
          {
            "node": "¿Hay admitidos?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Hay admitidos?": {
      "main": [
        [
          {
            "node": "Despachar cluster",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Ciclo de corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Despachar cluster": {
      "main": [
        [
          {
            "node": "Ciclo de corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Ciclo de corrida": {
      "main": [
        [
          {
            "node": "Esperar y reconciliar",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Esperar y reconciliar": {
      "main": [
        [
          {
            "node": "¿Corrida terminada?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Corrida terminada?": {
      "main": [
        [
          {
            "node": "Métricas",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Espera corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Espera corrida": {
      "main": [
        [
          {
            "node": "Cola y slots",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cola y slots": {
      "main": [
        [
          {
            "node": "Redespachar pendientes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Redespachar pendientes": {
      "main": [
        [
          {
            "node": "¿Hay pendientes?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Hay pendientes?": {
      "main": [
        [
          {
            "node": "Despachar pendiente",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Ciclo de corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Despachar pendiente": {
      "main": [
        [
          {
            "node": "Ciclo de corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Métricas": {
      "main": [
        [
          {
            "node": "Cerrar corrida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  }
}
