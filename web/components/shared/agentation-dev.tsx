"use client";

import { Agentation } from "agentation";

// Solo herramienta de desarrollo local: anotaciones visuales para agentes de IA.
// No se monta en producción (ver app/layout.tsx).
export function AgentationDev() {
  return <Agentation />;
}
