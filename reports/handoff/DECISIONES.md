# DECISIONES — bitácora del camino (H0 → H36)

Registro cronológico de decisiones con evidencia y alternativa descartada. Los jueces evalúan el camino (21 §1.6); este archivo es la fuente de `/metodo`.

| Hora | Decisión | Evidencia | Alternativa descartada |
|---|---|---|---|
| H0 2026-09-11 21:20 | Reto de fraude fiscal; sistema multi-agente con capa de falsos positivos (00). | Transcripción del juez: "no queremos acusar por una o dos transacciones raras"; "¿por qué esa sí y esta no?" | Detector de anomalías con score. |
| H0 21:25 | Leer la grabación Plaud del juez principal y volverla normativa (21). | `21-criterios-juez-e-inyeccion-en-vivo.md` §1 | Seguir 00 sin ajustar. |
| H0 21:30 | Añadir prueba de **inyección en vivo** como gate propio: corrida clonada + prioridad a clusters afectados + timeline/diff. | Juez: "si inyectamos datos sintéticos, ¿cómo reacciona y cómo se explica?" | Mutar el snapshot activo (rompe regla 10 y la reproducibilidad). |
| H0 21:32 | Proveedor `messages_api`. | Auditoría de `mrwh1th3/eximapp` ai-loop: 1 runner, ≤3 jobs, ≤14 min, sin métricas por request; inyección exige latencia baja y 8 tareas LLM. | `claude_code_actions` (queda como fallback documentado). |
| H0 21:33 | Cuatro builders en oleada 1 (db, runtime, webapp, prompts). | Usuario: "arranca YA todo el proyecto"; ownership disjunto. | Tres builders y prompts en espera. |
| H0 21:35 | Postgres 17 local (Homebrew) para migraciones; remoto solo por coordinador. | Sin docker/psql en la máquina. | Probar migraciones directamente en remoto. |
| H0 21:36 | Coordinador scaffolda `web/` y root con Husky; workers piden dependencias. | Regla: un único dueño de lockfiles. | Cada worker instala sus paquetes (colisión de lockfile). |
| H0 21:45 | Supabase `forense` **no creado**: la org free tiene 2/2 proyectos activos (clientes). Se registra bloqueo; el usuario decide pausar/upgradear. | Error `BadRequestException` del MCP al crear. | Pausar un proyecto de cliente (prohibido por regla de no tocar clientes). |
| H0 22:05 | Contratos 1.1.0 aditivos con el DTO de inyección antes de que existan consumidores. | `contracts/release.json` fingerprint 66a1414b…; 98 tests. | Dejar que db/webapp definieran cada uno su propio formato. |
| H0 22:10 | Oleada 1 con verificación adversarial por entrega (ownership, tests reproducidos, secretos). | Regla 18: el coordinador integra por diff y tests, no por resúmenes. | Integrar confiando en el reporte del builder. |
| H0 22:30 | Smoke real de la API vía n8n (`FORENSE_smoke_anthropic`, inactivo, una ejecución manual): confirma proveedor `messages_api` con credencial existente. | Ejecución 283972: modelo claude-sonnet-5, stop_reason tool_use, usage 645/45, 1089 ms. | Asumir saldo por suscripciones de programación (11 lo prohíbe). |
| H1 2026-09-11 23:35 | Usuario resolvió Supabase (proyecto dedicado `hackthon2026`), repo a privado, n8n 2.33.7 confirmado, número ElevenLabs se configurará desde la UI del proveedor. | `launch.config.json` targets; `gh repo edit --visibility private`. | Seguir en local hasta el final (perdería demo en producción). |
| H2 2026-09-12 00:20 | Oleada 1 se cortó sin JSON de entrega: las definiciones forense-* no incluían la herramienta StructuredOutput. Se añade a las 8 definiciones y la continuación usa agentes generales con modelo por rol sobre los mismos worktrees. | Notificación del workflow wf_51981d37: 4 agentes 'completed without calling StructuredOutput'; trabajo real presente en los worktrees. | Descartar los worktrees y empezar de cero. |
| H3 2026-09-12 01:30 | Integrada la oleada 1 en main: db (001–003, seed, harness 109 aserciones, generador y loader gen-v1), runtime (181 tests, 2 workflows JSON, MANIFEST), prompts (79 tests, manifest), webapp (20 rutas, 43 tests). | Veredictos de verificadores + reproducción por el coordinador. | Integrar sin reproducir tests. |
| H3 01:35 | `advance_case_if_ready(caso_id, paso, revision_expected)`: la barrera es por paso, así que la firma de 17 §4 gana el parámetro `paso`. db la implementa, runtime alinea JSON y dispatcher. | Hallazgo medio del verificador de runtime; implementación en memoria del dispatcher. | Mantener (caso_id, revision_expected) y deducir el paso dentro. |
| H3 01:36 | `ambito_techo='paquete'`: los techos de 12k/24k caracteres de 17 §7 aplican al paquete de contexto; el system prompt tiene su propio techo medido (≤10k). | Medición del ensamblador: system de especialista 9.1k de 12k dejaba 1.7k al paquete. | 'total' (dejaba sin pistas al especialista). |
| H3 01:37 | ACL de `forense_registrar_evidencia`: auditor Y especialistas (06 §11 herramientas); el Validador sigue siendo el único que valida. | Ambigüedad entre 03 (no la lista para especialistas) y 06 (sí). | Solo auditor. |
| H3 01:38 | Contratos 1.2.0: `CADENA` como prefijo de referencia, `trayectoria` opcional en paquetes de cierre, `corrida_cargada`/`inyeccion` en tipo_evento. db añade ambos eventos al check de bitácora en migración aditiva. | Solicitudes de db, prompts y runtime en sus entregas. | Dejar 'CICLO:cadena' y 'pista_cargada' con payload.evento_real para siempre. |
| H3 01:40 | Selector explícito de fuente de datos en la webapp (`NEXT_PUBLIC_DATA_SOURCE=fixture|supabase`, default fixture) para que la presencia de env de Supabase no active un origen aún no implementado. | Build de /datos falló en main con .env.local. | Borrar .env.local. |
| H4 2026-09-12 02:20 | `forense.ground_truth` queda legible por anon en el proyecto demo, como fija 05 (la vista de Estadísticas la necesita) y 19 (solo datos sintéticos). La garantía es que ninguna RPC del agente la lee (aserción sobre pg_proc.prosrc en db/tests). Si entra un dataset real, se retira el grant y se pasa a BFF. | Reporte del agente de migraciones remotas; 05 §RLS; 19 §Protección. | Retirar el grant ahora (rompería /estadisticas sin ganar nada con datos sintéticos). |
| H7 2026-09-12 05:10 | Oleada 2b integrada completa (db 009 + 14 pistas calibradas + eval; runtime recableado; editor persistente; webapp privada/realtime; QA 67+6; voz 52 tests; prompts variantes). Regeneración de manifest/workflows en la integración. | Veredictos: db/qa integrar; resto integrar_con_parches; suites en verde en main. | Integrar por partes. |
| H7 05:12 | Techo del system prompt: 10k caracteres para especialistas; 12k para roles de cierre (Auditor/Defensor/Redactor miden 10.0–10.3k). El techo del paquete sigue en 'paquete' 12k/24k. | Medición de prompts2; hallazgo baja del verificador. | Recortar el Redactor (perdería la sección Trayectoria). |
| H7 05:13 | Calibración de pistas con datos: T2 sincronía no_evaluable en gen-v1 (una sola hora del día en el generador); D3 Benford no_evaluable cuando >50% de comparables se desvían; F3 exige concentración ≥50% en un tercero. Selector 16/17 fraudes, 0/15 trampas; baseline dos pistas FPR 4/15. | eval/metricas.py sobre gen-v1 (verificado dos veces). | Dejar D3/T2/F3 marcando 77–88 de 100 RFC. |
| H7 05:14 | Contrato de ensamblado: ensamblar() exige motivo_reintento cuando intento≥1; variante_prompt lleva '+reintento:<motivo>'. Runtime lo adopta en la oleada 3. | Solicitud de prompts2; hallazgo medio del verificador. | Duplicar el ensamblado en el worker sin sello. |
| H9 2026-09-12 07:30 | Oleada 3 integrada: db 010/011 (26+ funciones del runtime con columnas exactas, revertir atómico, QA-002/003, métricas SQL iguales a eval), runtime cableado al 100% (preparar-sql ok=73) con e2e de dictamen determinista real, QA 131 integración + 7 e2e, editor persistente, webapp privada por perfil, voz con firma ElevenLabs, prompts con techos por rol. | Veredictos: runtime/qa/webapp/voice integrar; db/editor/prompts integrar_con_parches; suites verdes en main. | Integrar por partes. |
| H9 07:32 | QA-004 (alto, criterio del juez): tras clonar con inyección, armar_clusters puede dejar fuera a los RFC inyectados que no cruzan el selector de dos familias. Decisión: FORENSE_inyectar garantiza un cluster por RFC inyectado no cubierto vía forense.armar_cluster_para (ruta de investigación manual), y estado_corrida cuenta también clusters pendientes (hallazgo alto de db). Oleada 4 (db + runtime). | Pruebas de QA inyeccion-ensayos-bc; verificador de db. | Bajar el umbral del selector para los inyectados (rompería la regla de dos familias). |
| H9 07:33 | ensamblar() con intento≥1 sin motivo degrada a variante 'reintento:sin_motivo' con aviso; el runtime escribe meta.aviso_reintento en bitácora y usa meta.variante_prompt (no el rol) en el prompt_hash de respaldo. Oleada 4 (runtime). | Entrega de prompts3. | Lanzar excepción y tumbar el paso. |
| H11 2026-09-12 10:45 | Oleada 4 integrada (012 clusters garantizados + cola_corrida, 013 índices y F1 equivalente, runtime inyectar/sello de reintento, QA gate de rendimiento y build rancio, RUNBOOK 001–013). Veredicto de db 'devolver' por dos causas reales que se corrigen en hotfix: (a) los 42 s de F1 tras clonar son estadísticas rancias (ANALYZE tras clonar → ~2 s), (b) `casos.cobertura_completa` nunca se pone en true (todo dictamen sería no_concluyente). | Verificadores de db5 y qa4 con A/B medido (42 609 ms sin ANALYZE / 243 ms con). | Devolver la rama entera y perder 012/013 correctas. |
| H11 10:47 | estado_corrida conserva 6 columnas (cambiar el tipo rompe la reaplicación de 010); cola_corrida aporta cola_restante. FORENSE_corrida debe redespachar hasta vaciar la cola (runtime hotfix). | Verificador de db5 (alto). | Séptima columna en estado_corrida. |
| H11 10:48 | Ensayo (c): la trampa puede entrar al selector de dos familias; el criterio es el dictamen tras defensa (anomalia_explicada/no_concluyente), no la exclusión del selector; con proveedor simulado se declara 'requiere API real'. | Verificadores de db5 y runtime5. | Ajustar el paquete para que no dispare familias (falsearía la prueba de defensa). |
| H11 2026-09-12 12:10 | Hotfix integrado en main (db6 → runtime6 → qa5) y cerrado con dos migraciones del coordinador. 016: revoca el `execute` a PUBLIC que faltaba en 014/015, corrige la frontera que mide la cobertura y siembra `max_expansiones_caso=1`. Lo que faltaba NO se añade a 014/015: esos cuerpos ya están verificados byte a byte contra el remoto, y editarlos rompería esa comprobación. | Verificadores de db6/qa5; 459 aserciones con GEN=1 e integración 157/157 medidas en main. | Parchear 014/015 en sitio. |
| H11 12:12 | La cobertura mide la frontera que **pidieron las señales** (`forense.senales.frontera`), no `clusters.rfcs_frontera`. Esa columna es la lista de candidatos que deja `armar_clusters`: sobre un cluster real de gen-v1 son 70 RFC que nadie pidió mirar, así que la regla exigía una expansión inexistente y la cobertura no podía ser true jamás. Frontera residual tras gastar la cuota es una limitación declarada, no un hueco (03 §127). | Verificación sobre gen-v1: 40 RFC en el cluster, 70 candidatos; sin frontera pedida `true`, con frontera pedida y cuota `false`, cuota agotada `true`. | Dejar la lista de candidatos como criterio (todo dictamen seguiría en `no_concluyente`). |
| H11 12:14 | `max_expansiones_caso = 1`, sembrada. Tres lugares la leían con default de código 2 mientras 03 §127 y §219 dicen "máximo una expansión por cluster": código y documento normativo no pueden discrepar en silencio. | `db/016`; aserción con default imposible (99) para probar que la clave existe. | Cambiar el documento a 2. |
| H11 12:20 | **`anomalia_explicada` era inalcanzable.** El dictaminador exigía `pistas[].estado === 'refutada'`, un valor que ni el esquema (001: `disparada`\|`no_evaluable`) ni el contrato `entities.pista` admiten. El resultado de la defensa vive por caso en `casos.evaluacion_pistas`, y `paquete_auditor_final` lo leía por código cuando la clave es el ID de la pista (`pista_objetivo` es bigint por contrato), así que viajaba en null siempre. 017 corrige la clave; `dictaminar` exige que todas las pistas EVALUABLES estén `descartada`. Una `no_evaluable` no cuenta: nunca sostuvo nada. | Cadena leída en 001:185, contracts entities.pista, 010:480, 015:258, nodo «Paquete auditor final»; e2e (c) con cobertura arreglada daba `presuncion_alta`. | Quitar el `every` y aceptar descarte parcial (marcaría legítimas con una sola pista descartada). |
| H11 12:22 | El ensayo (c) se declara `requiere_api_real` **antes** de comparar el nivel si no corrió el Defensor (0 defensas resueltas). Sin la capa de descarte, el nivel que salga no es evidencia: ni alto prueba que el sistema falle, ni bajo que discrimine. No se simulan refutaciones. | `n8n/tests/e2e-inyeccion.mjs`; corrida en main: "(c) nivel observado presuncion_alta, no juzgable: no corrió el Defensor". | Simular un defensor que refute (fijaría el resultado que la prueba mide). |
| H11 12:24 | La cobertura se recalcula DESPUÉS del update en `guardar_dictamen`, no dentro del SET. Defecto de mi propio 016: las expresiones de un SET se evalúan contra la instantánea previa a la sentencia, así que leía los `pendientes` viejos y podía persistir `true` junto a una limitación declarada por ese mismo dictamen. | Aserción 017: dictamen con limitación nueva → cobertura false + evento `cobertura_recalculada` con motivo `dictamen`. | Dejar el cálculo en el SET (divergencia silenciosa entre columna y regla). |
